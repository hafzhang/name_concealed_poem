# ============================================
# 环境变量配置模板
# ============================================
# 复制此文件为 .env 并填入实际值

# ============================================
# Node 环境
# ============================================
NODE_ENV=development

# ============================================
# AI API 配置
# ============================================
# AI 服务 API 密钥（必填）
AI_API_KEY=your-api-key-here

# AI 服务基础 URL（可选，默认使用 OpenAI）
# AI_BASE_URL=https://api.openai.com/v1

# AI 模型名称（可选，默认 gpt-4）
AI_MODEL_NAME=gpt-4

# API 请求超时时间，单位毫秒（可选，默认 60000）
AI_TIMEOUT=60000

# ============================================
# 应用配置
# ============================================
# 应用 URL（可选，默认 http://localhost:3000）
APP_URL=http://localhost:3000

# ============================================
# 缓存配置
# ============================================
# 最大缓存大小，单位 MB（可选，默认 10）
CACHE_MAX_SIZE=10

# 图片缓存过期时间，单位小时（可选，默认 24）
CACHE_IMAGE_EXPIRY=24

# 字体缓存过期时间，单位小时（可选，默认 168 = 7天）
CACHE_FONT_EXPIRY=168
